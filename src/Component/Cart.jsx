import React, { useContext } from "react";
import { mycontext } from "../App";
import Card from "./Card";

const Cart = () => {
  
  return (
      <div>
        <Card />
      </div>
  );
};

export default Cart;
